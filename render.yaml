services:
  - type: web
    name: sentry-jira-slack-agents
    env: python
    runtime: python-3.13.12
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port ${PORT:-10000}
